#include <SFML/Graphics.hpp>

#include "Game.h"
#include <iostream>

int main()
{
    Vec2 vector(1, 2);
    std::cout << vector.x << ' ' << vector.y << '\n';

    //Game g("config.txt");
    //g.run();
}
